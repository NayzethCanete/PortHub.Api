using System; 
/*namespace PortHub.Api.Dtos
{
    public record FlightRequestDto(string FlightNumber, DateTime DepartureTime, DateTime ArrivalTime, long AirlineId);
    public record FlightResponseDto(long Id, string FlightNumber, DateTime DepartureTime, DateTime ArrivalTime, long AirlineId);
}*/